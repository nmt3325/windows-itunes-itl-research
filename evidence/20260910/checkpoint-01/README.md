# 2026-09-10 checkpoint01

This checkpoint preserves20 generated synthetic audio files and10 fresh native discovery ITL snapshots. It is NOT qualification of a new independent writer or complete semantic ITL support.

The parent re-read all20 media against the sealed byte/size/mtime manifest, and checked10 ITLs with the existing core for exact no-op, framing/count/reference checks and a unique complete master. This is a parent-side core check, not a second independent decoder or full68-field COM audit. Native-owner control logs and code are being reviewed separately.

The ITLs contain their original disposable Windows absolute Locations. Copying them to another machine does not relocate their media. Do not open them in a personal working library. Regenerate the native corpus or use a separately verified relocation procedure.

Media were generated by commit a087ee8, with encoder provenance and independent mutagen/ffprobe/PCM checks. That source is awaiting a small optimized-Python guard correction before integration. The media bytes are unchanged by that source-only correction. Producer and delivery hashes are separate provenance, not eligibility allowlists.

No user-provided original ITL/media/archive, application executable, authentication material or session transcript is included. Seven private original ITLs were re-read separately: all preserve outer bytes;4 pass the strict Library model and3 retain their prior structural rejection. No new private native repair has been qualified.

MANIFEST.json covers only this checkpoint directory, excluding itself. The historical root DELIVERY-MANIFEST.json belongs to git82c10a7 and must be verified in that frozen checkout, not against an expanded development branch. No historical manifest was rewritten.
