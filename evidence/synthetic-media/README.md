These are generated synthetic WAVs, not personal media. ITL binaries keep their original absolute Windows Locations. To replay the fixture-qualified CLI, materialize the manifest paths beneath the original fixture root read from the ITLs, in a disposable Windows environment, without overwriting existing files. Restore recorded mtimes where metadata validation requires them. Relocating the media alone does not relocate an ITL. No arbitrary-path support is claimed.
